# ==================== DJANGO SETTINGS ====================
# Generate a new secret key for production: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-your-secret-key-here

# Debug mode: Set to False in production
DEBUG=True

# Allowed hosts: Comma-separated list (e.g., localhost,127.0.0.1,yourdomain.com)
ALLOWED_HOSTS=localhost,127.0.0.1

# ==================== DATABASE SETTINGS ====================
# For SQLite (default)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# For PostgreSQL (uncomment and configure if needed)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=your_db_name
# DATABASE_USER=your_db_user
# DATABASE_PASSWORD=your_db_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# ==================== EMAIL SETTINGS (Gmail SMTP) ====================
# For Gmail:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: Google Account > Security > App Passwords
# 3. Generate an App Password and paste it below (16 characters, no spaces)

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here

# ==================== SITE SETTINGS ====================
# Site URL for email links (without trailing slash)
SITE_URL=http://localhost:8000

# ==================== TIMEZONE ====================
TIME_ZONE=Asia/Kolkata

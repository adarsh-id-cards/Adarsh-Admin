# ============================================================
# RENDER DEPLOYMENT ENVIRONMENT VARIABLES
# ============================================================
# Copy these to your Render Dashboard > Environment Variables
# or use render.yaml for Infrastructure as Code
# ============================================================

# ==================== REQUIRED FOR RENDER ====================
# NOTE: Render automatically sets RENDER_EXTERNAL_HOSTNAME
# You can also manually set RENDER=true if needed
# RENDER=true

# Django settings
DEBUG=False
SECRET_KEY=your-secure-random-secret-key-here

# Allowed hosts (Render provides your domain)
ALLOWED_HOSTS=.onrender.com,your-custom-domain.com

# ==================== DATABASE ====================
# Render PostgreSQL provides DATABASE_URL automatically
# If using external DB, set it manually:
# DATABASE_URL=postgres://user:password@host:5432/dbname

# ==================== SUPERUSER CREDENTIALS ====================
# These are used by startup.py to create the initial admin
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@mail.com
DJANGO_SUPERUSER_PASSWORD=admin123

# ==================== EMAIL (Optional) ====================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# ==================== SITE SETTINGS ====================
SITE_URL=https://your-app.onrender.com

# ==================== TIMEZONE ====================
TIME_ZONE=Asia/Kolkata

# ==================== STATIC FILES ====================
# Render serves static files via WhiteNoise (already configured)
